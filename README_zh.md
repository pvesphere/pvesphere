# PveSphere

[![license](https://img.shields.io/github/license/pvesphere/pvesphere-ui.svg)](LICENSE)

**中文** | [English](./README.md)

---

## 项目简介

PveSphere 是一个基于 Web 的 Proxmox VE (PVE) 多集群管理平台。它提供了一个统一的界面，用于从单一仪表板管理多个 PVE 集群、节点、虚拟机、存储和模板。

<img src="./docs/pvesphere-review03.gif" width="100%" />

## 为什么做 PveSphere？

Proxmox VE 是一个强大的开源虚拟化平台，但管理多个集群可能会遇到挑战：

- **多集群管理**：管理多个集群时需要在不同的 PVE Web 界面之间切换
- **统一监控**：难以获得跨所有集群的全局资源视图
- **模板同步**：跨节点手动同步 VM 模板既耗时又容易出错
- **操作复杂性**：跨集群管理虚拟机、存储和备份需要频繁切换上下文

PveSphere 通过提供一个**集中式管理平台**来解决这些问题，简化多集群操作并提高运营效率。

## 解决了什么具体问题？

### 🎯 核心痛点

1. **多集群管理**：统一界面管理多个 PVE 集群，无需在不同 Web UI 之间切换
2. **资源可见性**：从单一仪表板实时监控所有集群、节点、虚拟机和存储
3. **模板管理**：跨节点自动同步模板，支持共享存储和本地存储
4. **简化操作**：简化的虚拟机生命周期管理、迁移、备份和监控工作流
5. **更好的用户体验**：现代化、响应式的 Web 界面，比原生 PVE 界面更易用

### 🔍 具体使用场景

- 跨不同位置或环境管理多个 PVE 集群
- 集中监控和告警 PVE 基础设施
- 跨节点自动分发模板
- 简化的虚拟机配置和管理工作流
- 更好的资源可见性，用于容量规划

## 适合谁 / 不适合谁？

### ✅ 适合

- **多集群运维人员**：需要集中管理多个 PVE 集群的团队
- **中小型团队**：希望简化 PVE 操作的小型 DevOps 团队
- **模板密集型环境**：经常从模板部署虚拟机并需要自动同步的用户
- **资源监控导向**：需要更好了解跨集群资源使用情况的团队
- **开源爱好者**：偏好开源解决方案并希望避免供应商锁定的用户

### ❌ 不适合

- **单集群用户**：如果只管理一个 PVE 集群，原生 PVE Web 界面可能已经足够
- **大规模需求**：已经运营大规模 OpenStack 环境的团队
- **复杂 HA/DR 场景**：高级高可用性和灾难恢复需求可能需要专门的工具
- **仅 API 用户**：如果主要通过 API/CLI 与 PVE 交互，可能不需要 Web 管理界面

## 真实使用场景

### 场景 1：多环境管理
您运行开发、测试和生产三个 PVE 集群。无需登录三个不同的 PVE Web 界面，您可以从 PveSphere 的统一仪表板管理所有三个集群。

### 场景 2：模板分发
您的 VM 模板存储在本地存储上，需要让它们在多个节点上可用。PveSphere 自动化同步过程，节省数小时的手动工作。

### 场景 3：资源监控
您需要监控多个集群的资源利用率，以识别热点并规划容量。PveSphere 提供 CPU、内存和存储使用情况的集中视图。

### 场景 4：简化虚拟机操作
您的团队经常跨集群创建、迁移和管理虚拟机。PveSphere 提供简化的界面，减少所需的点击次数和上下文切换。

## 功能特性

### 核心功能

- **多集群仪表板**：实时概览所有集群、节点、虚拟机和存储资源
- **集群管理**：添加和管理多个 PVE 集群，集中配置
- **虚拟机生命周期管理**：创建、启动、停止、迁移、备份和恢复虚拟机
- **模板管理**：跨节点导入、同步和管理 VM 模板
- **存储管理**：监控存储使用情况和管理存储池
- **节点管理**：监控节点并通过终端代理访问节点控制台
- **虚拟机控制台访问**：通过 VNC/NoVNC 访问虚拟机控制台
- **资源监控**：实时指标和利用率跟踪

详细文档请访问：[https://docs.pvesphere.com](https://docs.pvesphere.com)

## 快速开始

### 环境要求

- Docker >= 20.10
- Docker Compose >= 2.0

### Docker 快速启动

```bash
# 克隆仓库
git clone https://github.com/pvesphere/pvesphere.git
cd pvesphere

# 构建并启动所有服务
make docker-compose-build

# 查看服务日志
make docker-compose-logs

# 停止所有服务
make docker-compose-down
```

### All-in-One 单容器部署

如果你只是想**快速体验**或以**单容器方式运行** PveSphere，可以直接使用预构建的 All-in-One 镜像（后端 + 前端打包在一个容器中）：

```bash
docker run -d \
  --name=pvesphere \
  --restart=always \
  -p 8080:8080 \
  pvesphere/pvesphere-aio:latest
```

然后在浏览器中访问：

- `http://localhost:8080`

### 默认登录

首次启动后，使用以下凭据登录：
- **邮箱**：`pvesphere@gmail.com`
- **密码**：`Ab123456`

> ⚠️ 请记住在首次登录后修改默认密码。

### 访问服务

- **API 服务**：http://localhost:8000
- **API 文档**：http://localhost:8000/swagger/index.html

详细的安装和配置说明，请访问：[https://docs.pvesphere.com](https://docs.pvesphere.com)

## 路线图

PveSphere 专注于**稳定性和正确性，而非功能开发速度**。以下高级路线图概述了计划中的改进：

### 核心改进

- **改进模板生命周期跟踪**：增强模板在整个生命周期中的状态可见性和管理
- **更安全的多集群编排**：在跨多个集群管理资源时提供更强大和可靠的操作
- **增强自动化工作流**：为常见操作任务提供简化的自动化能力
- **更好的可观测性和可审计性**：为所有操作提供全面的监控、日志记录和审计跟踪

### 计划功能

- **高级存储管理**：更全面的存储操作和管理能力
- **集群级虚拟机调度**：集群级别的智能虚拟机放置和调度机制
- **DRS（分布式资源调度器）**：用于动态资源重新平衡的二次调度能力
- **基于角色的访问控制（RBAC）**：细粒度的访问控制和权限管理
- **扩展监控和日志**：增强的监控能力和全面的日志管理

> **注意**：路线图项目优先考虑稳定性和正确性。功能将在经过充分测试和生产就绪时发布，而不是基于任意时间表。

## 文档

完整的文档，包括安装指南、API 参考和使用示例，请访问：

**📚 [https://docs.pvesphere.com](https://docs.pvesphere.com)**

## 贡献

欢迎贡献！无论是错误修复、功能增强还是文档改进，我们感谢您帮助 PveSphere 变得更好。

请随时提交 Issue 和 Pull Request。

## 许可证

[Apache License 2.0](LICENSE)

版权所有 © 2025-present PveSphere Contributors

## 联系方式

- **邮箱**：pvesphere@gmail.com
- **推特**：[@PveSphere](https://x.com/PveSphere)
- **GitHub**：[https://github.com/pvesphere/pvesphere](https://github.com/pvesphere/pvesphere)

---

## 相关项目

- [Proxmox VE](https://www.proxmox.com/) - 底层虚拟化平台
- [vue-pure-admin](https://github.com/pure-admin/vue-pure-admin) - 前端基础模板
- [Nunu](https://github.com/go-nunu/nunu) - 后端框架
